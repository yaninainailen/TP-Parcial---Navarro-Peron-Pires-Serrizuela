# User prompt — plantilla de cada corrida

Acá está la planilla de tickets pendientes de hoy (columnas: id, cliente, mensaje). Procesá cada
fila según tus instrucciones y devolveme la tabla completa, una fila de salida por cada fila de
entrada, en el mismo orden.

```csv
[contenido de tickets_entrada.csv de la corrida correspondiente]
```
